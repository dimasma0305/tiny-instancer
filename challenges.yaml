# Generated by instancer.builder
name: example-challenge
timeout: 900
containers:
- name: web
  security:
    read_only_fs: false
    cap_add:
    - CAP_CHOWN
    - CAP_FOWNER
    - CAP_SETGID
    - CAP_SETUID
  limits:
    memory: 256Mi
    cpu: '1'
  egress: true
  image: nginx:alpine
expose:
- kind: https
  container_name: web
  container_port: 80
---
name: build-challenge
timeout: 900
containers:
- name: app
  security:
    read_only_fs: false
    cap_add:
    - CAP_CHOWN
    - CAP_FOWNER
    - CAP_SETGID
    - CAP_SETUID
  limits:
    memory: 256Mi
    cpu: '1'
  egress: true
  image: web/build-challenge/app:latest
expose:
- kind: https
  container_name: app
  container_port: 8000
